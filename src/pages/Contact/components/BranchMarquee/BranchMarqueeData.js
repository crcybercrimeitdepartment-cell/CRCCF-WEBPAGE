export const branchMarqueeData = {
  defaults: {
    ariaLabel: "Branch locations"
  },
  card: {
    selectAriaLabel: (city, id) => `Select ${city} branch, ${id}`,
    activeText: "Active",
    inactiveText: "Branch node"
  }
};
